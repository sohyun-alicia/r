# 학생 A집단의 몸무게
weight <- c(74, 66, 61, 59, 70)

mean(weight)            # 평균 : 전체 숫자를 더한 후 개수로 나눈 값

median(weight)          # 중앙값 : 집단의 한 가운데에 있는 숫자, median

var(weight)             # 분산 : 편차를 제곱한 값의 평균값 (편차 : 각 값과 평균 사이의 차이)

sd(weight)              # 표준편차 : 분산 값에 루트를 적용한 값


